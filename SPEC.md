# SPEC.md — wiki-link-visualizer 仕様書

第一段階の作成仕様。実装の判断に迷ったら、この文書の「1. 目的」に立ち返ること。

---

## 1. 目的

**Wikipediaを読み歩く行為（wiki散歩）を、3D空間の移動として体験できるようにする。**

ある記事を起点に、関連記事へ次々と辿っていく。辿った軌跡が3D空間に残り、
「どこから来て、今どこにいるか」が空間的に見えている状態を作る。

### この目的から導かれる判断基準

迷ったときは以下で判断する。

- **一度に大量のノードを見せることは目的ではない。** 俯瞰して構造を分析する
  ツールではないので、ノードを増やすより「次にどれをクリックするか」を
  判断しやすくする方を優先する。
- **クリック前に、その記事が何かを判断できること**が体験の質を決める。
  記事名だけを見て賭けでクリックする状態は失敗。
- **3Dである理由は「空間を移動している感覚」にある。** 構造の読みやすさだけなら
  2Dの方が優れている。3D特有の読みにくさ（線の交差、奥行きの誤認）は
  UI側で補償する前提で設計する。

---

## 2. 第一段階のスコープ

### 作るもの

1. 記事名を入力して検索し、中心記事とその関連記事を3Dグラフとして表示する
2. マウスドラッグで回転、ホイールでズーム
3. ノードにカーソルを乗せると、その記事の概要（冒頭文＋サムネイル）が出る
4. ノードをクリックするとその記事へ進み、カメラが追いかける
5. 歩いた経路が履歴パネルに残り、いつでも遡って別の道へ行き直せる
6. 展開の深さに制限は設けない（クリックし続けられる限り伸びる）

### 作らないもの（第一段階では対象外）

- 非公式wiki（VTuber/アイドル系など、構造化APIがないもの）への対応
- カテゴリによる着色・クラスタリング
- URLへの状態保存・共有、2D/3D切替
- タッチ操作対応、レスポンシブの作り込み（幅 900px 未満でサイドバーを下に畳む基本形のみ入れてある）

（検索候補サジェストとデザインは 2026-09-19 の UI 指示書に沿って実装済み。4章・6章）

---

## 3. データ取得仕様

### 3.1 基本方針

**関連記事は「その記事からリンクされている記事のうち、内容が近い順の上位N件」とする。**

「内容が近い」の判定には、Wikipediaの検索エンジン(CirrusSearch)の
`morelike:記事名`(本文の類似度で関連記事を返す機能)を使う。

経緯:
- 最初の実装 `prop=links&pllimit=40` は記事名のコード順に先頭40件を返すだけで、
  関連度と無関係だった(日付記事だらけになる)
- 次に閲覧数順にしたが、MediaWiki API の `prop=pageviews` は
  **1リクエストで新たに5件分しか閲覧数を返さない**(サーバー側の制限)ため、
  数百件のリンク先の閲覧数を揃えることができず、「たまたまキャッシュされていた
  記事」が上位に並んでいた。「初音ミク」の隣に「シンガポール」「子供」が出る状態
- `morelike:初音ミク` とリンク先を突き合わせると、上位が
  「クリプトン・フューチャー・メディア」「鏡音リン・レン」「VOCALOID」となり、
  汎用記事も自然に落ちる。1リクエスト約2〜3秒

閲覧数はランキングには使わず、**表示するノードの分だけ**別途取ってノードの
大きさに使う(3.4)。

### 3.2 リンク先の取得

エンドポイント: `https://ja.wikipedia.org/w/api.php`

```
action=query
format=json
formatversion=2
origin=*                  … CORS許可(専用バックエンド不要)
generator=links           … リンク先ページをそのまま対象にする
titles=<記事名>
gplnamespace=0            … 標準記事名前空間のみ(Category:/Template:等を除外)
gpllimit=max              … 1回あたり最大500件
prop=info                 … 記事の長さ(length)を取る。3.3 の並べ替えで使う
redirects=1               … リダイレクトを解決してから探索
```

- 500件を超えるリンクを持つ記事は `continue` が返るので、
  **`MAX_CONTINUE = 3` 回**まで続けて取る(最大1500件)。それ以上は打ち切る
- 取得中はローディング表示を出し、取得済み件数を進捗として出す
- ユーザー入力の記事名は、先に `action=query&titles=&redirects=1` で正規化・存在確認
  してからリンク取得に進む。グラフ上のノード(API が返した正規名)から展開するときは
  この確認をリンク取得と並列に投げてよい

### 3.3 関連度の取得と並べ替え

```
action=query
list=search
srsearch=morelike:<記事名>
srnamespace=0
srlimit=500               … RELATED_LIMIT。上限は500
srprop=                   … タイトルだけあればよい
```

結果は関連度順に並んでいる。これをリンク先(3.2)と突き合わせて次の順に並べる。

1. **morelike に出てきたリンク先**を、morelike の順位で
2. **出てこなかったリンク先**を、記事の長さ(`length`)が大きい順で後ろに

上位 `POOL_SIZE` 件を候補プールとして 3.5 の抽選にかける。
後半は抽選の重みが小さいので、たまに顔を出す程度になる。

morelike と一致するリンク先が `GUARANTEED_TOP` 件に満たない場合
(短い記事・特殊な記事)は Console に警告を出す。

### 3.4 除外フィルタと閲覧数

**除外: 日付・年の記事**(どの記事からもリンクされていてノイズになる)

```
/^\d{1,4}年$/          例: 2007年
/^\d{1,2}月\d{1,2}日$/  例: 8月31日
/^\d{1,2}月$/
/^\d{1,4}年代$/        例: 1990年代
```

**除外: 一覧・曖昧さ回避**

- タイトルに「一覧」を含む
- タイトルが `(曖昧さ回避)` で終わる

**汎用ハブの除外は行わない。** 閲覧数順のときは「日本」「YouTube」のような
記事を閲覧数比で除外していたが(旧 `HUB_RATIO`)、morelike 順では関連の薄い
汎用記事は上位に来ないので不要になった。

**閲覧数(ノードの大きさに使う)**

エンドポイント:
`https://wikimedia.org/api/rest_v1/metrics/pageviews/per-article/ja.wikipedia/all-access/user/{記事名}/daily/{開始日}/{終了日}`

- 1記事=1リクエスト。**表示するノード(中心+抽選で選んだ40件)の分だけ**取る
- 集計は1日遅れで確定するので、終了日は昨日。そこから `PAGEVIEW_DAYS` 日分さかのぼる
- 記事名は空白を `_` にしてから `encodeURIComponent` する
- データが無い記事は 404 が返るので 0 として扱う
- 同時実行数は `VIEWS_CONCURRENCY` に抑える(投げすぎると 429 で拒否される)
- **グラフの表示を待たせない。** リンク取得が終わった時点でグラフを出し、
  閲覧数は裏で取って届いた順にノードを育てる
- 取れなかった記事は無視する(最小サイズのまま)。エラーは出さない
- **独自ヘッダを付けないこと。** `Api-User-Agent` 等を付けるとブラウザが
  事前確認(preflight)を挟み、REST API がそれに CORS ヘッダを返さないため失敗する
- 結果はメモリ上にキャッシュする(戻ったときに取り直さない)

### 3.5 リンクの抽選

**関連度順の上位を固定で出すと、同じ記事からは永遠に同じ顔ぶれしか出てこない。**
下位のつながりには一生たどり着けず、散歩として成立しない。
そこで、順位そのままではなく重み付きの抽選で選ぶ。

1. 除外・並べ替え後(3.3)の上位 `POOL_SIZE`(150件)を候補プールとして保持する
2. そのうち上位 `GUARANTEED_TOP`(10件)は**確定で出す**
   → 「初音ミク→ボーカロイド」のような当たり前の道が消えないようにするため
3. 残りの枠は、プールから重み付き非復元抽選で埋める
   → 重み = `1 / (順位 + SAMPLE_BIAS)`。順位が下がるほど小さいが**ゼロにはしない**

**抽選の乱数は種付き(12章)。** (`seed`, 解決後の記事名) が同じなら同じ顔ぶれになる。
同じ経路を設定違いで並べて比較するには、抽選が毎回変わっては困るため。
違う道が見たいときは `seed` を変える(以前は呼ぶたびに `Math.random` で変えていた)。
キャッシュがあっても抽選は再実行するが、種が同じなら結果も同じ。

ただし**一度出した結果は App 側で記憶しておくこと**(7章)。
記憶しないと、軌跡を遡ったときに以前と違う道が現れて経路が壊れる。

### 3.6 記事プレビューの取得

エンドポイント: `https://ja.wikipedia.org/api/rest_v1/page/summary/{記事名}`

- 記事名は `encodeURIComponent` でエスケープする
- CORS許可済み。1リクエストで冒頭文とサムネイルの両方が返る
- 使う項目: `extract`（冒頭の要約文）、`thumbnail.source`（画像URL）、
  `content_urls.desktop.page`（Wikipedia本文へのリンク）
- **取得結果はメモリ上にキャッシュする。** 同じノードに何度もカーソルが
  乗るので、都度リクエストしない

### 3.6b サイドバー用メタ情報の取得

サイドバー（6.2）に出す「カテゴリ / 被リンク数 / 更新日」。`fetchArticleMeta(title)`。
2リクエストを並列に投げる。

```
1. action=query&titles=<記事名>&prop=categories|info&clshow=!hidden&cllimit=5&redirects=1
   → categories[0].title から "Category:" を除いたものを主カテゴリに
   → touched(ISO 8601)の先頭10文字を更新日(YYYY-MM-DD)に
2. action=query&list=search&srsearch=linksto:"<記事名>"&srnamespace=0&srlimit=1&srprop=&srinfo=totalhits
   → searchinfo.totalhits を被リンク数に
```

- 補助情報なので**失敗してもエラーを出さない**。取れなかった項目は `—` を表示
- 結果はメモリ上にキャッシュする
- `prop=linkshere` は件数だけ取る手段がなく全件走査になるため、検索の件数を使う

### 3.6c 検索候補（オートコンプリート）

`fetchSuggestions(query)`。`action=opensearch&search=<q>&limit=8&namespace=0&format=json&origin=*`。
返り値の `[1]` が候補配列。存在する記事しか候補に出ないので、候補から選べば
「記事が見つからない」は起きない。

- 該当なし(0件)は空配列 → 検索欄の下に「該当する記事がありません」と出す
- 取得できなかった(HTTP エラー・通信失敗・打ち切り)は `null` → 何も出さない
  (通信失敗のたびに「見つかりません」と出すと、記事が無いと誤解させるため)

### 3.6d エラー時の挙動(通信・存在しない記事)

すべての Wikipedia への `fetch` は `FETCH_TIMEOUT_MS`(15秒)で打ち切る(`fetchWithTimeout`)。
サーバーが応答を返さないまま黙っても「FETCHING」のまま固まらない。

`apiGet`(MediaWiki API)が投げる Error の文言は、利用者が次に何をすればよいか分かるものにする:

| 状況 | 文言 |
|---|---|
| 記事が存在しない(`missing` / `invalid`) | 記事が見つかりませんでした: 「…」(表記を確認するか、検索欄の候補から選んでください) |
| HTTP 429 | Wikipedia へのアクセスが集中しています(HTTP 429)。しばらく待ってから再試行してください |
| HTTP 5xx | Wikipedia 側で障害が起きている可能性があります(HTTP nnn)。時間をおいて再試行してください |
| HTTP 200 だが JSON でない(メンテナンス画面等) | Wikipedia APIの応答を読み取れませんでした(JSON ではない応答) |
| 打ち切り | Wikipedia API の応答が 15 秒以内に返りませんでした。時間をおいて再試行してください |
| 接続失敗 | Wikipedia APIへの接続に失敗しました(ネットワーク/CORSの問題の可能性があります) |

画面側(`App.jsx`):

- グラフが空のとき(検索前・URL の `start` が不正)はグラフエリア中央に `ERROR …` を出し、
  その下に通常の案内文を残す(真っ黒な画面の左上の小さな文字では気づけない)
- グラフがあるとき(展開に失敗)は左上のステータス行に `ERROR …` を出し、**グラフはそのまま残す**
- URL の `path` の途中で失敗したら、そこまでの経路でグラフを組み、URL もそこまでに書き直す
- 閲覧数・プレビュー・メタ情報・検索候補は補助なので、失敗しても画面にエラーを出さない
- 3.5 の抽選、3.4 の閲覧数取得、3.6 のプレビューは影響を受けない

`tests/wikipedia.test.js` に上記の自動テストがある(`npm test`)。手動の再現手順は `README.md`。

### 3.7 定数(すべて1箇所にまとめて定義する)

| 定数 | 初期値 | 意味 |
|---|---|---|
| `MAX_LINKS` | 40 | 1記事から展開するノード数 |
| `POOL_SIZE` | 150 | 抽選の母集団にする候補数(3.5) |
| `GUARANTEED_TOP` | 10 | 抽選せず必ず出す上位件数(3.5) |
| `SAMPLE_BIAS` | 10 | 抽選の重みの効き方。大きいほど平坦=よりランダム(3.5) |
| `RELATED_LIMIT` | 500 | morelike で取る関連記事の件数。上限500(3.3) |
| `MAX_CONTINUE` | 3 | リンク先一覧の継続取得の上限回数。1回500件(3.2) |
| `PAGEVIEW_DAYS` | 3 | 閲覧数を集計する日数(3.4) |
| `VIEWS_CONCURRENCY` | 8 | 閲覧数を同時に取りにいく件数(3.4) |
| `PREVIEW_DELAY_MS` | 300 | ホバーしてからプレビューを取りに行くまでの待ち |
| `VISIBLE_LABELS` | 24 | 同時に表示するラベルの最大数 |
| `LABEL_CANDIDATES` | 60 | ラベルの重なり判定にかけるノードの最大数 |
| `VIEWS_SCALE_MIN` / `MAX` | 10 / 100000 | 一次ノードの半径に対応させる閲覧数の下端・上端(6.4) |
| `TRAIL_KEEP` | 2 | 子ノードを残す訪問記事の件数(7章) |
| `MAX_NODES_WARN` | 300 | この数を超えたら注意を表示 |
| `FETCH_TIMEOUT_MS` | 15000 | 1リクエストを諦めるまでの時間(3.6d) |
| `REPULSION` / `REPULSION_RANGE` | 2600 / 320 | 反発の強さと計算する距離の上限(12.2) |
| `SPRING_K` / `SPRING_LENGTH` | 0.012 / 55 | リンクのバネの硬さと自然長(12.2) |
| `CENTER_K` / `DAMPING` / `ALPHA_DECAY` | 0.006 / 0.86 / 0.99 | 中心への引力・減衰・収束の速さ(12.2) |
| `MAX_SPEED` / `ALPHA_MIN` | 14 / 0.015 | 発散の歯止めと収束の判定。VizConfig には出さない |
| `SIM_STEPS_PER_SEC` / `SIM_MAX_STEPS_PER_FRAME` | 60 / 4 | 固定刻みシミュレーション(12.1)。VizConfig には出さない |
| `SPAWN_SPREAD` | 120 | 新規ノードの初期配置の広がり。VizConfig には出さない |
| `MOBILE_BREAKPOINT_PX` | 900 | この幅未満をモバイル用レイアウトとみなす。`App.css` のメディアクエリと揃える(4章) |
| `MOBILE_MAX_PIXEL_RATIO` | 1.5 | モバイルでの描画解像度(devicePixelRatio)の上限(8章) |

UI 指示書（2026-09-19）で追加した表示・モーション用の定数。単位はすべて
**画面上のピクセル**か秒。意味は `constants.js` の日本語コメントを正とする。

| 定数 | 初期値 | 意味 |
|---|---|---|
| `NODE_PX_CURRENT` / `_MAX` | 14 / 15.5 | 起点の半径。呼吸で往復(6.5) |
| `NODE_PX_PRIMARY_MIN` / `MAX` | 6 / 9 | 一次ノードの半径。閲覧数(対数)で幅を持たせる(6.4) |
| `NODE_PX_VISITED` | 7 | 訪問済み(中空)ノードの半径 |
| `NODE_PX_SECONDARY` / `NODE_SECONDARY_OPACITY` | 4 / 0.5 | 二次ノードの半径と不透明度 |
| `BREATH_PERIOD_S`, `RING_PX_MIN/MAX`, `RING_OPACITY_MAX/MIN` | 3.2, 20/26, 0.55/0.15 | 起点の呼吸と外周リング(6.5) |
| `HIT_RADIUS_PX` | 16 | クリック当たり判定の半径 |
| `EDGE_PRIMARY_OPACITY` / `EDGE_HOVER_OPACITY` / `EDGE_WEAK_OPACITY` | 0.5 / 1.0 / 0.15 | 実線・ホバー・破線の不透明度 |
| `EDGE_DASH_SIZE` / `EDGE_GAP_SIZE` | 1.5 / 3.5 | 破線の実部・空白(ワールド座標) |
| `HOVER_DIM_RATIO`, `HOVER_SCALE`, `HOVER_TRANSITION_S` | 0.3, 1.35, 0.2 | ホバー時の減光倍率・拡大率・補間時間(6.1) |
| `LABEL_PX` / `LABEL_CURRENT_PX` / `LABEL_GAP_PX` / `LABEL_MAX_CHARS` | 12 / 13 / 6 / 16 | ラベルの文字高・間隔・打ち切り文字数(6.3) |
| `GRID_RING_RADII_PX`, `GRID_RING_OPACITIES`, `GRID_RADIAL_*`, `GRID_ROTATION_PERIOD_S` | 60〜300, 0.05〜0.03, 8本, 140 | 背景グリッド(6.7) |
| `PACKET_COUNT` / `PACKET_PERIOD_S` / `PACKET_STAGGER_S` / `PACKET_PX` | 12 / 2.6 / 0.4 / 2.2 | データパケット(6.7) |
| `DRIFT_BACK` / `DRIFT_FRONT` | 26s (+7,−5) / 19s (−4,+3) | パララックスドリフト(6.7) |
| `TRAVEL_MS` / `TRAVEL_EASE` / `TRAVEL_DIM_OPACITY` | 700 / (.22,.85,.25,1) / 0.15 | クリック遷移(6.5) |
| `FOLLOW_LERP` | 0.06 | カメラ追従の追いつき速度(6.5) |
| `ZOOM_STEP` / `ZOOM_TWEEN_MS` | 1.3 / 250 | ズームボタン1回の倍率と移動時間 |
| `SUGGEST_DEBOUNCE_MS` / `SUGGEST_LIMIT` | 200 / 8 | 検索候補(3.6c) |
| `BREADCRUMB_COUNT` | 3 | パンくずに出す直近件数(6.6) |

---

## 4. 画面仕様

デザインは UI 指示書「Wireframe Terminal」案（リポジトリ外の別文書）に従う。
**黒背景 `#000` に白の不透明度階調だけ。アクセントカラー・角丸・グラデーション・絵文字は使わない。**
枠線は全て `1px solid`。書体は Space Grotesk（見出し・記事タイトル）と JetBrains Mono（UI・数値・ラベル）
の2つで、Google Fonts から `index.html` の `<link>` で読む。CSS 変数（`--bg --fg --border --border-faint --muted` 等）は
`App.css` の `:root` に定義する。全面に薄いスキャンラインを重ねる（`pointer-events: none`）。

基準 1280×800 の左右分割固定レイアウト。

| 要素 | 位置 | 内容 |
|---|---|---|
| トップバー | 上端 64px、下境界線 | 左: ロゴ `WIKI://NODES` ＋ 検索欄（`>` プロンプト、プレースホルダ「記事を検索」、点滅カーソル、候補ドロップダウン）。右: `NODES` `EDGES` `DEPTH` の統計値 ＋ リセットボタン（SVG アイコン） |
| グラフエリア | 左（残り幅） | three.js の canvas。左上に取得状況/エラー（等幅 11px）、左下にパンくず（6.6）、右下にズーム +/−（30px 矩形） |
| 右サイドバー | 右 340px 固定、常時表示 | `SELECTED NODE` ラベル → 記事タイトル（22px/700）→ 区切り線に挟まれたメタ情報（カテゴリ / 被リンク数 / 更新）→ 抜粋（13px）→ Wikipedia リンク → 「隣接記事 (N)」リスト（スクロール可） |

### レイアウトの3モード

画面の形で3つに分ける。判定は `src/utils/layoutMode.js`、見た目は `App.css` のメディアクエリ。
**両方に同じ条件を書く**(CSS は JS の定数を参照できないため)。片方だけ直すとずれる。

| モード | 条件 | サイドバー |
|---|---|---|
| `wide` | それ以外 | 右に 340px 固定。初期状態は開 |
| `narrow` | `max-width: 899px`(`MOBILE_BREAKPOINT_PX`) | グラフの下に畳む(高さ 40vh 固定)。初期状態は開 |
| `short-landscape` | `(orientation: landscape) and (max-height: 500px)`(`SHORT_LANDSCAPE_MAX_HEIGHT_PX`) | グラフに**重ねる**ドロワー(右端・幅 `min(340px, 45vw)`)。初期状態は閉 |

`short-landscape` は `narrow` より**後ろに書いて優先させる**。844x390 のような横向きの
スマートフォンは両方の条件に当てはまるため、順序で決めている。

横向きのスマートフォンを幅だけで判定すると破綻する:

- 844x390 は幅900px未満なので縦積みになり、折り返したトップバーと 40vh の
  サイドバーでグラフが 100px 前後しか残らない
- 932x430 は幅900px以上なのでPCレイアウトになり、340px のサイドバーでグラフが狭い

どちらも原因は「高さが足りない」ことなので、高さで判定する。

`short-landscape` ではトップバーを1行 44px に詰め、統計値(NODES/EDGES/DEPTH)は隠す。
パンくずとズームボタンは、ドロワーを開いている間はその左へ逃がして押せる位置に置く。

**ドロワーの開閉で canvas をリサイズしない。** `position: absolute` でグラフの上に
重ねており、`.graph-area` の寸法は開閉で変わらない。リサイズするとカメラの縦横比が
変わってマップ全体が動く(下の「サイドバーの高さ」と同じ理由)。

### サイドバーの格納

全モードで格納できる(`<button>` + `aria-label` + SVG アイコン)。
`wide` と `narrow` では閉じるとグラフ領域が広がる。**このリサイズは利用者が押した結果なので許容する。**
開閉状態は保存しない(リロードで初期値に戻る)。画面の形が変わったとき(端末の回転)も、
そのモードの初期値に戻す。閉じている間は `aria-hidden` と `inert` を付ける
(幅を 0 にしただけでは Tab キーで中のボタンに入れてしまうため)。

**開閉にはモーションを付ける。** 一瞬で切り替えると「パッと画角が変わる」感じになり、
実機で確かめたときに気持ちよさが落ちると指摘があった(2026-09-25)。

| モード | 動かすもの | canvas のリサイズ |
|---|---|---|
| `wide` | 幅 340px ⇄ 0(内側の余白と境界線も一緒に) | 起きる。ResizeObserver が追従してグラフが滑らかに広がる |
| `narrow` | 高さ 40vh ⇄ 0 | 同上 |
| `short-landscape` | `transform: translateX(100%)` で横にスライド。寸法は変えない | **起きない**(重ねているだけなので) |

時間と曲線は `App.css` の `--panel-motion-duration`(280ms)と `--panel-motion-ease`。
曲線はクリック遷移のカメラ(`constants.js` の `TRAVEL_EASE`)と同じ値にして、画面全体で
動きの質を揃えている。時間はカメラ(700ms)より短くする ― パネルの開閉は移動ではなく
操作への応答なので、待たされる感じを出さない。

`prefers-reduced-motion: reduce` のときはすべて即時に切り替える。

### サイドバーの高さ

`narrow` ではサイドバーの高さを `--sidebar-stacked-height`(40vh)に**固定**する。
中身に合わせて伸び縮みさせると、ホバーで記事が切り替わるたびにグラフ領域の高さが変わり、
canvas がリサイズされてマップ全体が動いてしまう。高さは `100dvh`(モバイルのアドレスバー分を除いた実表示高さ)。
検索・ノードのタップ・戻る・ズームはモバイルでも同じ操作で動く(ホバーは無いので、サイドバーは常に現在地)。
モバイルでの本格的な再構成(リスト表示への切り替え等)は未着手(11章)。

`NODES` / `EDGES` は表示中のグラフの数、`DEPTH` は trail の長さ。

### ノードの見た目

色は使わない。**大きさ・塗り/中空・線種・不透明度**に別々の意味を割り当てる。
両方に同じ意味を持たせると、表示できる情報が1種類に減ってしまう。

| 要素 | 意味 |
|---|---|
| 大きさ | 現在地からの距離（起点 > 一次 > 二次）。一次の中だけ閲覧数（6.4） |
| 塗り / 中空 | 未訪問は白塗り、訪問済み（軌跡上の起点以外）は白い輪郭だけ |
| 線種 | 起点につながる線は実線、それ以外は破線 |
| 不透明度 | 奥のもの（二次ノード・破線）ほど薄い |
| 外周リング＋呼吸 | 現在地（6.5） |

現在の構造への読み替え: **起点＝現在地（trail 末尾）、一次＝現在地に直接つながるノード
（現在地の展開先と、ひとつ前の訪問記事）、二次＝それ以外（`TRAIL_KEEP` で残っている過去の
訪問記事の子など）**。

| 種別 | 半径（画面 px） | 塗り | 不透明度 |
|---|---|---|---|
| 起点 | 14（呼吸で 14→15.5→14、周期 3.2 秒） | 白 | 1 |
| 一次（未訪問） | 6〜9（閲覧数の対数） | 白 | 1 |
| 一次（訪問済み） | 7 | 中空（輪郭のみ） | 1 |
| 二次 | 4 | 白 / 訪問済みは中空 | 0.5 |

| エッジ | スタイル | 不透明度 |
|---|---|---|
| 主要（起点↔一次） | 実線 1px | 0.5（ホバー時 1.0） |
| 弱い（それ以外） | 破線 | 0.15 |
| 背景グリッド（同心円・放射状） | 極細 | 0.03〜0.05 |

**大きさは画面上のピクセルで一定**（`Sprite` の `sizeAttenuation:false`）。
カメラが寄っても引いてもノードとラベルの見た目の大きさは変わらない。
ラベルは JetBrains Mono 12px `#cfcfcf`、ノードの右（画面外に出るなら左）。起点のラベルだけ
13px/700 で真下・中央揃え。

## 5. 操作仕様

| 操作 | 挙動 |
|---|---|
| ドラッグ | 視点を回転 |
| ホイール / ズーム +− ボタン | ズーム（ボタンは注視点に向かって `ZOOM_STEP` 倍ずつ寄せる/引く） |
| ノードにホバー | 隣接ハイライト（6.1）＋サイドバーがそのノードに切り替わる（6.2） |
| 未訪問ノードをクリック | その記事へ進む。取得後、カメラが 0.7 秒でそのノードへ飛び、到着後は追従する（6.5） |
| 訪問済みノードをクリック | その記事まで軌跡を遡る（そこから先の経路は捨てる）。カメラは同様に飛ぶ |
| サイドバーの隣接記事をクリック | ノードクリックと同じ処理で進む（小さい球を狙わなくてよい） |
| パンくずの項目をクリック / ← ボタン | 軌跡を遡る / 一手戻る |
| Backspace | ひとつ前の記事に戻る（検索欄にフォーカスがある間は無効） |
| 検索欄 | 200ms デバウンスで候補を出す。↑↓で選択、Enter で確定、Esc で閉じる、候補クリックでも確定 |
| 検索実行 | すべて破棄して、新しい記事から歩き始める |
| リセットボタン | グラフを空にして検索前の状態に戻す |

遷移中（取得中と、カメラが飛んでいる 0.7 秒）のクリックは無視する（二重遷移の防止）。

ドラッグとクリックの区別: ポインタを押してから離すまでの移動量が5px以内、
かつ600ms以内のときだけクリックと判定する（回転操作の誤爆防止）。

---

## 6. UI追加仕様

第一段階で入れるのは以下の3つ。いずれも「3Dの読みにくさを補償する」ためのもので、
これが無いと3Dである意味が薄くなる。

### 6.1 ホバー時の隣接ハイライト

**カーソルを乗せたノードと、それに直接つながるノード・エッジだけを通常表示にし、
それ以外を減光する。**

3Dグラフ最大の弱点は、線が交差して構造が読めなくなること。これ一つで
可読性が大きく変わる。

- ホバー中のノード: 1.35 倍に拡大し、ラベルを `#fff` にする
- ホバー中のノードにつながるエッジ: 不透明度 1.0
- 非隣接のノード・エッジ: 不透明度を `HOVER_DIM_RATIO`（0.3）倍まで落とす
  （黒背景なので「白を暗くする」＝「不透明度を下げる」と見た目が同じ。
  エッジは頂点カラーの明るさで、ノードは `SpriteMaterial.opacity` で表す）
- 非隣接のラベル: 非表示にする
- 切り替えは `HOVER_TRANSITION_S`（0.2 秒）で補間する。`applyHighlight` は目標値だけを決め、
  `updateVisuals` が毎フレーム目標へ寄せる
- 隣接判定用に「ノードid => 隣接ノードidの集合」を保持しておく。
  毎回リンク配列を走査しない

### 6.2 サイドバー（記事プレビュー）

**右サイドバーは常時表示。ホバー中はそのノード、ホバーが外れたら現在地の記事を出す。**

目的1に書いたとおり、クリック前にその記事が何かを判断できることが
この体験の質を決める。

- ホバーしてから `PREVIEW_DELAY_MS`（300ms）待ってから取得を開始する。
  カーソルが通過しただけのノードを取りに行かないため。現在地の記事は即時に取る
- 表示項目: `SELECTED NODE`（ホバー中は `HOVER NODE`）ラベル、記事名（全文。ラベルと違い省略しない）、
  メタ情報（カテゴリ / 被リンク数 / 更新日。3.6b）、冒頭文（`extract`。6 行で打ち切る）、
  Wikipedia本文へのリンク、隣接記事リスト
- 隣接記事 = そのノードの `expansions`（展開済みなら）。未展開なら
  「クリックして展開すると表示されます」の一行を出す。各項目は `<button>` で、
  クリックするとノードクリックと同じ処理で進む
- 取得中は記事名と `…` / 「読み込み中…」を出す。空パネルにしない
- 取得に失敗しても**エラーを表示しない**。補助機能なので、黙って `—` を出せばよい
- 結果（summary・meta とも）はキャッシュする
- クリック遷移では、**取得完了と同時にサイドバーを切り替え**、パンくずは到着（0.7 秒後）で更新する
- サムネイル画像は出さない（モノクロのデザインに合わないため見送り）

### 6.3 ラベルの間引き

**全ノードに常時ラベルを出すと、数が増えた時点で文字が重なって読めなくなる。**
表示するラベルを絞る。

表示するのは以下のみ:

- 中心記事のノード
- 展開済みのノード
- ホバー中のノードとその隣接ノード
- 上記以外で、**優先度（起点 > 一次 > 二次）順、同じ優先度ならカメラから近い順に最大 `VISIBLE_LABELS` 件**

判定は毎フレームではなく、**200msごと**に再評価する（毎フレームだと重い）。
画面上の矩形を求めて、先に確定したラベルと重なるものは捨てる。

- ラベルは**画面上 12px 固定**（`Sprite` の `sizeAttenuation:false`）。以前はワールド座標で
  固定サイズだったため、近づくと巨大化していた
- ノードの右に置き、右端からはみ出す場合は左に置く。起点だけは真下・中央揃え。
  位置は `Sprite.center`（アンカー）で決める
- 不透明度はノードに追従する（二次ノードのラベルは薄く、ホバーの減光にも従う）
- 文字列は `LABEL_MAX_CHARS`（16）文字で打ち切り、末尾に `…` を付ける。
  全文はサイドバーで見られるので問題ない
- テクスチャは Web フォント読込後に描き直す（読込前に描くと代替書体になる）

### 6.4 一次ノードの大きさ＝閲覧数

閲覧数は表示するノード分だけ REST API から取る（3.4）。届いた順に球が育つ。

- **必ず対数で割り当てること。** 閲覧数は記事間で1万倍以上違うため、
  線形にすると巨大な球ひとつと見えない点の集まりになる
- `VIEWS_SCALE_MIN`〜`MAX` の閲覧数を `NODE_PX_PRIMARY_MIN`〜`MAX`（6〜9px）に対応させる
- 幅を持たせるのは**一次ノードの未訪問のものだけ**。起点・訪問済み・二次は固定サイズ
  （大きさの主な意味は「現在地からの距離」で、閲覧数はその中の副次的な差にとどめる）

副次的な効果として、「あえて小さい球を選ぶ」という遊び方が生まれる。
3.5の抽選と合わせて、マイナーな記事へ向かう動機になる。

### 6.5 現在地の明示、クリック遷移、カメラ追従

現在地は**最大のノード（14px）＋呼吸＋外周リング**で示す。
リングは半径 20→26→20px、不透明度 0.55→0.15→0.55 を周期 3.2 秒で往復する。
以前の黄色い「光の輪」は廃止した（色を使わないため）。

**クリック遷移（`travelTo`）**: クリックしたノードへカメラが `TRAVEL_MS`（0.7 秒）で飛ぶ。
イージングは `cubic-bezier(.22,.85,.25,1)` 相当。見る角度と距離は保ち、注視点だけを移す。
目的地はレイアウトで動き続けるので、トゥイーンの終点は毎フレーム取り直す。
移動中は目的地以外のノード・主要エッジを `TRAVEL_DIM_OPACITY`（0.15）まで減光する。
**ノードの位置の入れ替えはしない**（指示書のモックとは違い、カメラだけが動く）。

到着後はカメラが現在地を**追い続ける**（`followNode`）。
展開直後のノードはレイアウトが落ち着くまで動き回るので、一度きりの移動だと
着いた頃には対象がずれている。

- 注視点を現在地へ毎フレーム少しずつ寄せ、カメラも同じだけ平行移動する
- **見る角度と距離は変えない。** ユーザーが作った視点を壊さない
- **手動でカメラを操作した時点で追従をやめる。** 勝手に動き続けると操作を奪われる

### 6.6 履歴パンくずと「戻る」

古い枝を畳む仕様（7章）の結果、**畳んだ道へ戻る手段が画面上に無くなる**。
パンくずがその入口になるので、畳む仕様とセットで必須。

- グラフ左下に、直近 `BREADCRUMB_COUNT`（3）件を `/` 区切りで出す。
  末尾（現在地）だけ `#fff`、それ以前は `#666`、区切りは `#444`。
  それより前がある場合は先頭に `…` を出す
- 項目をクリックすると、その記事を現在地にして**そこから先の経路は捨てる**
  （来た道を引き返して、別の道へ行き直す操作）
- 「←」ボタンと Backspace キーで一手戻る
- 更新はカメラの到着（`TRAVEL_MS` 後）に合わせる。検索直後とリセットは即時

### 6.7 背景グリッドとモーション

いずれも「静止画に見えない程度」に抑える。派手にしない。

- **背景グリッド**: 起点を中心に同心円（半径 60/120/180/240/300px、不透明度 0.05〜0.03）と
  8 方向の放射状ガイド線（長さ 420px、0.03）。カメラに正対するビルボードとして起点に追従する。
  `depthTest` を切って最初に描くので、何にも重ならない。インタラクション対象外
- **起点の呼吸**: 6.5
- **データパケット**: 起点→一次エッジ上を半径 2.2px の点が周期 2.6 秒で流れる。
  対象は **現在地の `expansions` の先頭 `PACKET_COUNT`（12）件**（確定枠→抽選枠の順なので
  関連度上位）。開始を 0.4 秒ずつずらす。不透明度 0→1→1→0。線が減光しているときは一緒に落とす
- **ホバー発光**: 6.1
- **パララックスドリフト**: 背景レイヤー（グリッド・二次ノード）は 26 秒周期で (+7,−5)px、
  前景レイヤー（起点＋一次）は 19 秒周期で (−4,+3)px を往復する。放射状ガイド線は 140 秒で 1 回転。
  ドリフトは描画位置にだけ加え、レイアウト座標は動かさない。エッジは両端のノードの描画位置を結ぶ
  ので、レイヤーが違っても線が離れない。当たり判定も描画位置で行う

---

## 7. 状態モデル

**グラフは差分で足し引きせず、毎回「訪問した記事の列(trail)」から組み立て直す。**
こうすると「戻る」が trail を短く切るだけで実現でき、進む／戻るのどちらでも
同じ結果になることが保証される。

| 状態 | 内容 |
|---|---|
| `trail` | 訪問した記事idの列（古い順）。歩いた軌跡そのもの |
| `expansions` | 記事id => その記事を展開したときに**実際に出した**リンク先 |
| `viewsOf` | 記事id => 閲覧数。ノードの大きさに使う。裏で届くたびに追記され、グラフを組み直す |
| `graphData` | trail から組み立てた表示用のグラフ |
| `crumbTrail` | パンくずに出す軌跡。カメラの到着（`TRAVEL_MS` 後）に合わせて trail より遅れて更新する |
| `hoveredId` | ホバー中の記事id。サイドバーの表示対象は `hoveredId ?? currentId` |
| `sidebar` | サイドバーに出している記事の `{ id, summary, meta }`。`undefined` は取得中、`null` は失敗 |
| `previewCache` / `metaCache` | 記事id => プレビュー（REST summary）/ メタ情報（3.6b） |
| `packetIds` | データパケットを流す先（現在地の `expansions` の先頭 `PACKET_COUNT` 件）。`currentId` から導出 |
| `travelingRef` | カメラが飛んでいる間 true。二重クリックのガード |
| `loading` / `loadingId` / `progress` / `error` | 取得状態 |

| `config` / `presetName` | 表示パラメータ(12章)。URL から読み、デバッグパネルで変えると URL にも書く |

**`expansions` に抽選結果を記憶しておくことが必須。** 抽選は `seed` を変えると
結果が変わるので、記憶せずに再構築すると、遡ったときに以前と違う道が現れて
経路そのものが壊れる。

### グラフの組み立て（散歩の軌跡モデル）

- **訪問した記事そのものは消さない** … 歩いた軌跡になる(`trailEnabled=false` のときは現在地とその子だけ)
- **直近 `TRAIL_KEEP` 件の訪問記事は、その子（リンク先）も出す** … この先の選択肢
- **それより古い訪問記事の子は畳む** … 選び終わった選択肢はもう要らない
- 連続する trail 同士は必ず線で結ぶ（畳んでも経路は切れない）

**ノードのidは記事タイトル（リダイレクト解決後）をそのまま使う。**
クリックしたノードのタイトルと API が返す解決後のタイトルが食い違う場合があるので、
軌跡に積むのは必ず**解決後**の名前にすること。

## 8. 非機能・性能

- 力学シミュレーションはノード間総当たり（O(n²)）。**ノード300件程度までを
  想定**する。それを超えると1フレームの計算が重くなる
- レイアウトが収束したら計算を止める（`alpha` が閾値を下回ったら停止）。
  グラフが変化したら再開する
- ノード数が `MAX_NODES_WARN = 300` を超えたら、操作パネルに
  「ノードが増えすぎています」と注意表示を出す。**止めはしない**
  （整理機能は次段階のため、まず警告だけ）
- スマートフォン(`narrow` または `short-landscape`。4章)では描画解像度(devicePixelRatio)を
  `MOBILE_MAX_PIXEL_RATIO = 1.5` に抑える。スマートフォンは DPR 3 が普通で、
  そのまま描くと毎フレーム塗る画素数がデスクトップの数倍になるため。
  **幅だけで判定しない。** 横向きにすると幅が 900px を超える端末があるため。
  端末を回したときにも見直せるよう、判定はリサイズのたびに行う。
  ノード数や力学計算はモバイルでも変えない(同じ URL で同じ配置になる、を崩さないため)
- 3D 描画に失敗した(WebGL が使えない)場合は canvas の代わりに文言を出す(真っ暗にしない)

---

## 9. 技術構成

- React + Vite
- 3D描画: **Three.js 本体を直接使用**
- 依存パッケージは `react` / `react-dom` / `three` と、デバッグパネル用の `leva`(12章)のみ

**`react-force-graph-3d` / `3d-force-graph` / `three-forcegraph` /
`three-render-objects` は使わないこと。** 内部依存のバージョン解決が壊れており、
「データは取れるのに画面が真っ暗」という再現済みの不具合を起こす。
`overrides` による固定も効かない(下位依存が `^1.29` のような緩い指定で最新版を引くため)。

### ファイル構成

```
src/
├── main.jsx
├── App.jsx                   … 状態管理、検索・展開ロジック
├── App.css                   … デザイントークン（:root の CSS 変数）とレイアウト
├── api/
│   ├── wikipedia.js          … リンク・関連度・閲覧数の取得、抽選（3.2〜3.5）、メタ情報（3.6b）、検索候補（3.6c）
│   └── summary.js            … 記事プレビューの取得（3.6）
├── constants.js              … 3.7 の定数をここに集約
├── config/
│   ├── presets.ts            … VizConfig 型とプリセット(12章)
│   └── urlState.js           … URL クエリ ⇔ 設定・経路 の読み書き(12章)
├── utils/
│   └── prng.js               … 種付き乱数(mulberry32)。配置と抽選に使う
└── components/
    ├── Graph3D.jsx           … 3D描画。ハイライト・ラベル間引き・グリッド・パケット・遷移・追従もここ
    ├── TopBar.jsx            … ロゴ・検索欄（候補付き）・統計値・リセット
    ├── Sidebar.jsx           … 右サイドバー（6.2）
    ├── Breadcrumb.jsx        … 左下の履歴パンくず（6.6）
    ├── ZoomControls.jsx      … 右下のズーム +/−
    └── DebugPanel.jsx        … leva のデバッグパネル(?debug=1 のときだけ)
tests/
├── wikipedia.test.js         … API のエラー処理・抽選の決定論(node:test。依存なし)
└── urlState.test.js          … URL クエリの読み取り
```

`index.html` で Google Fonts（Space Grotesk / JetBrains Mono）を `<link>` で読む。npm 依存ではない。

---

## 10. 完成の判定基準

1. 「初音ミク」で検索して3Dグラフが表示される
2. 出てくる関連記事が、**日付記事や無関係な記事で埋まっていない**
   (「初音ミク」なら VOCALOID・クリプトン・鏡音リン・レンが上位に出る)
3. **`seed` を変えると同じ記事でも違う顔ぶれが出る**（3.5の抽選が効いている。同じ seed なら同じ顔ぶれ）
4. **一次ノードの大きさに差があり、有名な記事ほど大きい**（6.4）
5. 現在地が最大の球＋呼吸するリングで一目で分かり、クリックするとカメラが 0.7 秒で飛んでからついてくる
6. ノードにカーソルを乗せると、隣接だけが明るくなり、そのノードが少し大きくなる
7. ノードにカーソルを乗せると、右サイドバーに記事名・カテゴリ・被リンク数・更新日・概要・隣接記事が出る。
   外すと現在地の記事に戻る
8. パンくず・Backspace・訪問済み（中空）ノードのクリックで過去の記事に戻れ、そこから別の道へ進み直せる
9. ノードが増えても**ラベルが重なって読めなくなっていない**（6.3）。ズームしてもラベル・球の大きさが変わらない
10. 検索欄に入力すると候補が出て、↑↓ Enter で確定できる
11. 画面に色（白の階調以外）が出ていない
12. ブラウザのConsoleにエラーが出ていない
13. 存在しない記事名・Wikipedia の 429/5xx で画面が真っ白にならず、日本語のエラーが出る(3.6d)
14. `npm test` が通る

## 11. 次段階の候補（着手しない）

第一段階が動いてから、優先度をつけ直して検討する。

- **「ランダムに飛ぶ」ボタン**（散歩のサイコロ。目的1に対する費用対効果が高い）
- 相互リンク（A→BとB→Aの両方がある関係）を太く描く
- レスポンシブの作り込み（モバイルでのグラフ/サイドバーの再構成）
- URLへの状態保存と共有
- 非公式wiki対応（構造化APIがないため、方式の検討から必要）

---

## 12. 比較の土台(VizConfig・URL・デバッグパネル)

UI の調整は試行回数が多い。「改善したのか、配置が変わっただけなのか」を判別できるよう、
コードを書き換えずに設定を切り替え、同じ経路を並べて比較できる状態を先に作った。

### 12.1 レイアウトと抽選の決定論

- 乱数はすべて種付き(`src/utils/prng.js`、mulberry32)。**`Math.random` をレイアウト・抽選に使わない**
- ノードの初期位置は (`seed`, 記事名) から決まる。同一座標のずらしも `seed` 由来の乱数列
- 力学シミュレーションは固定刻み(60 ステップ/秒、1 フレーム最大 4 ステップ)。
  フレームレートに依存させない
- リンクの抽選は (`seed`, 解決後の記事名) の乱数(3.5)
- URL から経路を復元するときは、**全部取り終えてから一度だけ**グラフを組む。
  途中で描画すると通信の待ち時間の分だけシミュレーションの進みが変わり、配置がずれる

同じ (開始記事, 経路, 設定) なら、同じステップ数を進めた配置は完全に一致する。
`?debug=1` のとき `window.__viz.step(600)` で同期的に進め、`window.__viz.positions()` で照合できる。

`window.__viz` で使えるもの:

| 呼び方 | 内容 |
|---|---|
| `positions()` | 全ノードの座標と `__sim`(進めたステップ数・alpha) |
| `step(n)` | レイアウト計算を同期的に n ステップ進める |
| `config()` | 今の VizConfig |
| `trail()` | 今の経路 |
| `set({ repulsion: 9000 })` | 設定を変える。leva を触らずに挙動を確かめたいとき |

なお**ブラウザのタブが非表示のときは `requestAnimationFrame` が止まる**ので、
放置しても収束しない。照合するときは `step(n)` で明示的に進めること。

### 12.2 VizConfig とプリセット(`src/config/presets.ts`)

| 項目 | 意味 | 効く時期 |
|---|---|---|
| `nodeLimit` | 表示ノード数の上限 | Phase 2(今は記録のみ) |
| `neighborLimit` | 1記事から展開するリンク数(旧 `MAX_LINKS`) | 次に展開する記事から |
| `edgeMode` | `radial`(現在地→隣接の線だけ) / `induced`(表示中ノード同士の全リンク) | `induced` は Phase 1 |
| `colorMode` | `mono` / `category` | `category` は Phase 3 |
| `trailEnabled` | 軌跡(訪問済みとその子)を残すか | 即時 |
| `seed` | 乱数の種 | 配置は即時作り直し。抽選は次の展開から |
| **layout** | 力学レイアウト。下記7項目 | 即時(配置は作り直さず計算を再開) |
| `repulsion` | ノード同士が押し合う強さ。大きいほど広がる | 即時 |
| `repulsionRange` | 反発を計算する距離の上限 | 即時 |
| `springK` | リンクのバネの硬さ。大きすぎると振動する | 即時 |
| `springLength` | バネの自然長(隣接ノードの狙いの距離) | 即時 |
| `centerK` | 原点へ引き戻す力。0 で際限なく広がる | 即時 |
| `damping` | 速度の減衰。小さいほど早く止まる | 即時 |
| `alphaDecay` | alpha の減衰率。小さいほど早く収束する | 即時 |
| **visual** | 見た目。下記4項目。配置には影響しない | 即時 |
| `visibleLabels` | 同時に表示するラベルの最大数 | 即時 |
| `edgePrimaryOpacity` | 実線(起点↔一次)の不透明度 | 即時 |
| `edgeWeakOpacity` | 破線(弱いエッジ)の不透明度 | 即時 |
| `followLerp` | カメラ追従の追いつき速度 | 即時 |

**`seed` の変更と `layout` の変更は区別する。**
`seed` は初期位置からの作り直し(velocity も 0 に戻す)。
`layout` は**今の位置を保ったまま** `alpha` を 1 に戻して計算を再開する。
こうするとパラメータを動かしたときに今の形からどう変形するかが見え、比較しやすい。

各項目の最小値・最大値は `presets.ts` の `RANGES` に定義し、
leva のスライダーの端と URL の丸めに同じ値を使う(パネルと URL でずれないようにするため)。
上限・下限は「この外に出すと比較にならない(発散する・描画が壊れる)」ところに置いている。
理由は `presets.ts` のコメントを正とする。

`MAX_SPEED` / `ALPHA_MIN` / `SIM_STEPS_PER_SEC` / `SIM_MAX_STEPS_PER_FRAME` / `SPAWN_SPREAD` は
`constants.js` に置くだけで VizConfig には出さない。配置の好みではなく
安定性(発散の歯止め)やフレーム処理に効く値で、触ると環境ごとに結果が変わるため。

プリセット: `current`(今の見た目。回帰確認用に**削除しない**)、`mesh`(ネットワーク向け。Phase 1 以降で本領)。
**`current` の値は `constants.js` の既定値と完全に一致させること。** ずれると比較の土台が壊れる
(`tests/urlState.test.js` で一致を検査している)。

### 12.3 URL クエリ(`src/config/urlState.js`)

```
?preset=mesh&nodeLimit=64          設定(プリセット + 個別上書き)
?start=流体力学&path=マグネシウム,ウラン  経路(開始記事と辿った記事)。歩くたびに replaceState で更新
?debug=1                           デバッグパネルと window.__viz を出す
```

2つのタブで別プリセット・同じ経路を開いて並べて比較する、が狙い。

### 12.4 デバッグパネル(`leva`)

`?debug=1` のときだけ表示。各値を変えると即時反映し、URL にも書く。
`leva` はこのためだけの依存で、通常表示では読み込まれても何も描かない。
